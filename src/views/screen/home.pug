extends ../layout
include ../mixins/video

block contents
  .video__grid
    each video in videos
      +video(video)
    else
      span No video found.

block scripts
  script(src="/static/scripts/video_grid.js") 